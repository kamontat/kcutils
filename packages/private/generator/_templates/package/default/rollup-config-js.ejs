---
to: <%= h.location.skipIf(compiler === "tsc", h.location.buildPath(category, name, "rollup.config.js")) %>
---

const config = require("@kcconfig/rollup-config");
const pkg = require("./package.json");

module.exports = config.initial(
  config.buildCommonJSModuleJS(pkg)<% if(browser){ -%>,<% } -%>
  <% if(browser){ -%>config.buildBrowser(pkg)<% } -%>
);
