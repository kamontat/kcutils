---
to: <%= h.location.buildPath(category, name, "README.md") %>
---

# <%= h.name.toPackageName(category, name) %>

> This package was generated by @kcprivate/generator@<%= h.local.version %>

## Development Stack

1. primary language: `typescript`
2. build and compile: `<%= compiler %>`
3. test: `jest`
4. mutator test: `strker-mutator`
5. code style: `eslint`

## To do list after generate new package

1. If you want this package to shown in document website, add package name to `typedoc.json`
